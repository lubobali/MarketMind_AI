# MarketMind AI — Dependencies
# Note: PySpark, Delta Lake, MLflow are pre-installed on Databricks clusters (DBR 15+)
# These are for local development, testing, and CI
# Python version: 3.12 | Databricks Runtime: 15.4 LTS

# Data Sources
yfinance==1.2.0
newsapi-python==0.2.7

# NLP / Sentiment
textblob==0.19.0
vaderSentiment==3.3.2

# Visualization
plotly==6.6.0

# UI (Databricks only — not required for CI)
# gradio==4.44.1

# Utilities
requests>=2.31.0
pandas==2.3.3
numpy==2.4.2

# Dev / CI
ruff==0.15.5
pytest==9.0.2
